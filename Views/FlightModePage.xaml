<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiApp1.Views.FlightModePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:MauiApp1.Controls"
    Title="Flight Mode"
    Shell.NavBarIsVisible="False">

    <Grid>
        <BoxView Background="{StaticResource CosmicBackgroundBrush}" />
        <controls:StarfieldView />

        <Grid Padding="18,16" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button
                Text="← Back"
                BackgroundColor="{StaticResource CosmicSurface}"
                TextColor="{StaticResource TextPrimary}"
                CornerRadius="18"
                Padding="14,10"
                HorizontalOptions="Start"
                Clicked="OnBackClicked" />

            <Label
                Grid.Row="1"
                Text="Flight Simulator Activated - Drag to rotate &amp; pinch to zoom"
                FontSize="16"
                TextColor="{StaticResource NeonCyan}" />

            <GraphicsView x:Name="HudView" Grid.Row="2" />
        </Grid>
    </Grid>
</ContentPage>

